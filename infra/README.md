# Infrastructure Notes

This directory is reserved for future infrastructure-as-code artifacts (Terraform, deployment manifests, etc.). The current local workflow runs directly on the host using `uvicorn` and `npm run dev`, so there are no infra assets yet.
